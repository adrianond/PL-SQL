SELECT * FROM EMPLOYEES where EMPLOYEE_ID = 145;
--GROUP BY
SELECT JOB_ID, DEPARTMENT_ID, SUM(SALARY) AS SALARIO FROM EMPLOYEES GROUP BY JOB_ID, DEPARTMENT_ID; 

SELECT JOB_ID, DEPARTMENT_ID, SUM(SALARY) AS SALARIO FROM EMPLOYEES WHERE SALARY >= 13900 GROUP BY JOB_ID, DEPARTMENT_ID;